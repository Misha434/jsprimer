"use strict";
// `name`という名前の変数を宣言
const name = "azu";
// `NAME`という名前の変数を宣言
const NAME = "AZU";

console.log(name);
console.log(NAME);

mistypedVariable = 42; // ReferenceError: mistypedVariable is not defined